import React from 'react';
import LoginForm from './LoginForm';

export const metadata = {
  title: 'Sign In — DineFlow CRM',
  description: 'Access the DineFlow Restaurant CRM & Operations suite.',
};

export default function LoginPage() {
  return <LoginForm />;
}
